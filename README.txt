
1. npm i gulp -g   	// Installing gulp global mode ( if you never installed gulp on your pc ) ( be sure to install gulp v4.0.0 or higher )
2. npm i gulp      	// Now instal gulp localy ( be sure to install gulp v4.0.0 or higher )
3. npm i           	// Install all modules
4. gulp            	// Launch your project with Watcher ( browserSync included)
5. gulp build    	// To build a final version of the project